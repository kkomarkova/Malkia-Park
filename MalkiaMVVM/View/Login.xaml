<Page
    x:Class="MalkiaMVVM.View.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MalkiaMVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ViewModel="using:MalkiaMVVM.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <ViewModel:LoginViewModel/>
    </Page.DataContext>
       
    <Grid Background="#FFFFE1B4" >

        <Grid.RowDefinitions>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="110*"/>
            <RowDefinition Height="150*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <StackPanel x:Name="stkpanelLogin" Margin="15,0,15,0" Visibility="{Binding LoginFieldsVisibility}">
                <StackPanel>
                    <TextBox PlaceholderText="Username" Text="{Binding Username, Mode=TwoWay}" Margin="0,5,0,0"/>
                    <PasswordBox PlaceholderText="Password" Password="{Binding Password, Mode=TwoWay}" Margin="0,5,0,0"/>
                </StackPanel>
                
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="113,10,15,0">
            <Button x:Name="btnLogin" Margin="5,5,5,5" Content="LOG IN" Command="{Binding LogInCommand}" Width="159" />
            <Button x:Name="btnLogout" Margin="5,5,5,5" Content="LOG OUT" Command="{Binding LogOutCommand}" RenderTransformOrigin="0.491,1.224" Width="156" />
        </StackPanel>
        
    </Grid>
</Page>
